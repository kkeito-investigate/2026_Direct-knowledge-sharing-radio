# スラッシュコマンド

このリポジトリでは、議論の速記と再現性のためにスラッシュコマンドを使います。

## 一覧
- `/3行` : 「3行でお前がやったこと教えて」を意味する。直近の作業要約を3行で返す。
- `/前3行` : 「その前にお前がやったことを3行で教えて」を意味する。直前より一つ前の作業要約を3行で返す。

## 運用ルール
- 迷ったら `/3行` を使う
- 要約は必ず3行で返す
- 必要なら日付を入れる
